# Frontend Environment Variables (for local development)
# In production, leave empty to use same-origin API calls

# Backend API URL (only needed for local dev)
NEXT_PUBLIC_API_URL=http://localhost:8000
